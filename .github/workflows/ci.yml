name: CI Workflow
on:
  push:
    branches:
      - develop
      - master
jobs:
  unit-test:
    strategy: 
      matrix: ['macos-latest', 'ubuntu-latest']
    runs-on: 
    steps:
      - uses: actions/checkout@v2
      - run: cd ut && make && ./main

