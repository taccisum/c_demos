name: CI Workflow
on:
  push:
    branches:
      - develop
      - master
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd ut && make && ./main

